name: Build Miaki

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt update
        sudo apt install -y make g++ git wget

    - name: Install VitaSDK
      run: |
        git clone https://github.com/vitasdk/vdpm
        cd vdpm
        ./bootstrap-vitasdk.sh -y
        echo "VITASDK=$HOME/vitasdk" >> $GITHUB_ENV
        echo "$HOME/vitasdk/bin" >> $GITHUB_PATH

    - name: Build Miaki with make
      run: |
        make
